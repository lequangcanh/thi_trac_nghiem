<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to t(".logo"), sv_root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if sinh_vien_logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_sinh_vien.ho_ten_day_du %>&nbsp;
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", "#" %></li>
              <li><%= link_to "Settings", "#" %></li>
              <li class="divider"></li>
              <li>
                <%= link_to t(".logout"), sv_logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", sv_login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
